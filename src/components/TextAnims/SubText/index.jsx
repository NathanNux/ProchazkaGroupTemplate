export default function SubText({text, className}) {
    return (
        <div className={className}>
            {text}
        </div>
    )
}

// WIP: This needs a really simple opacity animation, nothing fancy, needs to be simple